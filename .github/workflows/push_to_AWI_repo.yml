# This is a basic workflow to help you get started with Actions

name: CI

on: workflow_dispatch   

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |      
            git config --global user.email "apatel54@ua.edu"
            git config --global user.name "arpita0911patel"
            git config --global credential.helper cache
            git clone https://${{secrets.SECRET_TO_PUSH_CODE_FROM_ARPITA0911PATEL_TO_AWI_REPO}}@github.com/AlabamaWaterInstitute/alabamawaterinstitute_website
            git add .
            git commit -m "github-actions[bot]-$(date)"
            git push
      
#       - name: Push directory to another repository
#         uses: cpina/github-action-push-to-another-repository@v1.5.1
#         env: 
#           API_TOKEN_GITHUB: ${{ secrets.SECRET_TO_PUSH_CODE_FROM_ARPITA0911PATEL_TO_AWI_REPO }}        
#         with:                
#           source-directory: '.'        
#           destination-github-username: github-actions[bot]
#           destination-repository-name: 'AlabamaWaterInstitute/alabamawaterinstitute_website'
#           user-email: 'apatel54@ua.edu'          
